# Zonovia Mobile (`mobile/`)

**Out of scope for Phase 0.** Per the [architecture blueprint](../docs/architecture/Zonovia-Architecture-Blueprint.md) §15/§30, the Flutter + `drift` (SQLite) offline-first mobile app lands in Phase 5 ("Mobile & Offline"), after Asset Core and baseline tracking exist for it to scan against. This directory is a placeholder so the repo layout matches the blueprint's monorepo structure (§26) from day one.
