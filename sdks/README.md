# Zonovia SDKs (`sdks/`)

**Out of scope for Phase 0.** Per the [architecture blueprint](../docs/architecture/Zonovia-Architecture-Blueprint.md) §26/§30, Connect API client libraries ship once external integrators actually exist to consume them (Phase 7 — Workflow & Integrations, and beyond). This directory is a placeholder so the repo layout matches the blueprint's monorepo structure from day one.
