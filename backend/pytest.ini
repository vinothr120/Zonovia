[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    postgres: requires a live Postgres database (docker compose up -d postgres) — see tests/test_tenant_isolation_postgres.py. Excluded from the default SQLite-backed unit tier via `-m "not postgres"`.
